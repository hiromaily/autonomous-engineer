# AI フレンドリー監査 Lite レポート

**日時**: 2026-03-14 15:08:41
**ツール**: AI-Friendly Audit Lite（シングルパス、サブエージェントなし）

---

## リポジトリ概要

**リポジトリ**: autonomous-engineer
**規模**: 中規模（ソースファイル 275 件: TS 151、JS 124）
**技術スタック**: TypeScript（主要）、Bun ランタイム
**構成**: `orchestrator-ts/` をメインパッケージとするモノレポ（`adapters/`、`application/`、`domain/`、`infra/` レイヤー）

---

## チェック結果

| # | チェック項目 | スコア | 概要 |
|---|------------|--------|------|
| 1 | リポジトリプロファイル | — | 中規模 TypeScript モノレポ。`orchestrator-ts/` がメインパッケージ、Bun をパッケージマネージャとして使用 |
| 2 | AI エージェント設定ファイル | **B** | `CLAUDE.md` + `.claude/rules/`（3 ファイル）+ スキル 1 件 — Claude のみ対応。Cursor・Copilot・Windsurf のルールは未整備 |
| 3 | エントリーポイントドキュメント | **C** | `README.md` に概要・背景は記載されているが、ディレクトリ構造・セットアップ手順・開発コマンドの参照が欠如。全 60 行 |
| 4 | ディレクトリ予測可能性 | **A** | `orchestrator-ts/` は即座に発見可能。`git` 機能コードは `adapters/git/`・`application/git/`・`domain/git/` に整理されており予測しやすい |
| 5 | 規約ドキュメントレベル | **A** | `.claude/rules/typescript.md` に全 `bun` コマンドを明示。npm 使用を禁止する `DO NOT` 記述あり。コード生成はこのプロジェクトには非該当 |
| 6 | パターン一貫性 | **B** | `adapters/`・`application/`・`domain/`・`infra/` が `git`・`safety`・`tools` のサブディレクトリ名を約 50% 共有。DDD レイヤリングは一貫しているが、`infra/` のみ `config`・`events`・`memory` と乖離 |
| 7 | アーキテクチャドキュメント | **A** | `docs/architecture/architecture.md` をはじめ 6 件以上の実質的な内容を持つアーキテクチャドキュメントが存在。日本語版（`docs/ja/`）も整備済み |

---

## 総合グレード: A（16/21）

スコア計算: A=3、B=2、C=1（最大 21 点）

---

## 改善アクション（優先順）

### 1. エントリーポイントドキュメント — `README.md`（チェック 3、C 評価）

`README.md` はビジョン・背景の記述のみで 60 行。以下を追加する:

- **ディレクトリ構造**セクション: `orchestrator-ts/{adapters,application,domain,infra}/` のレイアウトを図示
- **クイックスタート**セクション: `bun install && bun run test` 等の主要コマンドを記載
- **参照リンク**: `docs/README.md` および `CLAUDE.md` へのリンク

AI エージェントがこのリポジトリに初めてアクセスした際、コードの配置場所や実行方法の手がかりがないため、最も優先度の高い改善項目。

### 2. AI エージェント設定ファイル — マルチエージェント対応（チェック 2、B 評価）

現在 Claude Code のみ対応。以下を検討:

- Codex/OpenAI エージェント向けに `AGENTS.md` をリポジトリルートに追加
- Cursor ユーザー向けに `.cursor/rules/typescript.md`（`.claude/rules/typescript.md` のコピーまたはシンボリックリンク）を追加

---

## 次のステップ

- 上記の改善を実施（優先: `README.md`）
- Lite を再実行してグレードの向上を確認
- Lite が **S（19-21）** に達したら、フルバージョン（`/ai-friendly-audit-full`）に移行してより詳細な評価を実施
